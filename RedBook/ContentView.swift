import SwiftUI

struct ContentView: View {
    var body: some View {
            VStack(alignment: .center) {
                // 静态的Text
                Text("Hello Swift!")
        }
    }
}

#Preview {
    ContentView()
}
